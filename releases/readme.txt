From 0.9.6 "google-webfonts-for-woo-framework" has been deprecated. The next
version will be "google-fonts-for-woo-framework" (without the "web").

Somehow the "web" got dropped when the plugin was submitted to WordPress, so
we are just going to run with that instead.
